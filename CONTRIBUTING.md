# 如何提交案例（同學適用）

歡迎將你找到的公司法／證券交易法實務案例提交到本專案，作為課堂學習與評分的一部分。

## 兩種提交路徑，擇一或都做

- **路徑一：自創新案例** — 自己找一則新聞事件，判斷該歸哪個章節，新建一份完整講義檔案。請照下面「提交方式」的步驟操作。
- **路徑二：補充既有框架** — 有些講義是老師先建好架構、部分區塊留白（會標示「待補充」），開放同學查證後補完。這種情況直接在原檔案的骨架位置補內容即可，**不用另建新檔案**，也不用跑「提交方式」裡新增 `LECTURES` 陣列那一步（檔案已經存在、已經被登錄過了）。

不確定自己找到的案例算哪一種、或該歸哪個章節，可以先跟老師確認再動手。

## 提交方式

1. 先確認你的案例對應到哪一個章節，參考 `index.html` 裡的 `CHAPTERS` 分類清單（例如：`5-3` 股東會、`5-4` 董事及董事會、`5-11` 解散、合併及分割 等，完整清單請直接看 `index.html` 原始碼）
2. 案例檔案一律放在 `lectures/` 資料夾下（**平面結構，不分子資料夾**），格式為 HTML，可參考現有講義的排版（例如「中工經營權爭奪案」）
3. 檔名建議格式：使用英文＋連字號，例如 `company-law-art170-192-zhonggong-case.html`（避免中文檔名分享連結時出現亂碼）
4. 新增檔案後，還要在 `index.html` 的 `LECTURES` 陣列裡新增一筆物件，包含：條號、標題、日期、對應章節的 `chapterKey`、檔案路徑（`file`）、以及 `author`（教師示範請填 `teacher`，同學提交請填 `student`）
5. 提交方式（依教師公告為準）：
   - 透過 Pull Request 提交，等待審核合併，或
   - 依教師指示的其他管道繳交

## 不熟悉 GitHub 的同學看這裡（Pull Request 完整步驟）

同學沒有本專案的直接寫入權限，**第一步一定要先 Fork**（複製一份到自己帳號），不能跳過。全程都在網頁上操作，不需要安裝任何軟體或使用指令。

1. 打開本專案首頁：`https://github.com/mjib007/company-law-cases`，點畫面右上角的 **Fork** 按鈕，將本專案複製一份到你自己的 GitHub 帳號下
2. 進入**你自己帳號下**的那份 Fork（網址會變成 `github.com/你的帳號/company-law-cases`），找到要新增或補完的講義檔案（例如 `lectures/company-law-merger-shinshin-ctbc-case.html`），點檔名進去
3. 點檔案內容右上角的鉛筆圖示（Edit this file）
4. 在網頁編輯器裡找到要補完的段落，直接打字修改或新增內容（記得依 [具名規則](#ai-協助與具名規則) 加上遮蔽版姓名學號）

   **範例**：假設你要在「課堂討論」某一題底下新增自己的看法，找到類似這樣的地方：

   ```html
   <ul class="strategy-proposals" id="discussion-q1">
     <li class="todo-placeholder">（尚無同學投稿）</li>
   </ul>
   ```

   在 `</ul>` 前面新增一行你自己的 `<li>`：

   ```html
   <li>你的看法內容。（陳○明，******456）</li>
   ```

   如果你是第一個投稿，記得把原本 `<li class="todo-placeholder">（尚無同學投稿）</li>` 那一行刪掉。

5. 編輯完畢後，捲動到頁面最下方的「Commit changes」區塊，填寫這次修改的簡短說明（例如：補完課堂討論Q1）
6. 選擇 **Create a new branch for this commit and start a pull request**（不要選「Commit directly to the main branch」），接著點綠色的 **Propose changes** 按鈕
7. 系統會自動跳到「Open a pull request」頁面，確認左上角的方向是「base: mjib007/company-law-cases」← 「compare: 你的帳號/你的分支」，在下方描述欄簡述你補完了哪一段，再點 **Create pull request**
8. 送出後不用做任何事，等老師審核（審核流程與時程請見下方「AI 審查流程說明」）

**常見狀況：**
- 如果同一份講義後來又有更新，你可以直接回到步驟2重新編輯，或是回到你Fork的repo首頁，點 **Sync fork** 同步最新版本後再開始編輯，避免漏看老師後續補充的內容
- 忘記加遮蔽版姓名學號、或打錯字：可以在同一個 PR 裡直接對著檔案再編輯一次、重新 commit，不需要關掉重開一個新的 PR
- 找不到 Fork 按鈕，或畫面跟這裡描述的不一樣：GitHub介面偶爾會調整，直接截圖問老師即可

## 內容格式，建議固定包含以下區塊

- 案例來源（新聞出處連結）
- 事件時間軸（或爭點問答）
- 對應條文（條號逐一附連結，法規 pcode 請務必查證，不可憑印象套用）
- 課堂討論方向
- 延伸閱讀（如需要）

重點是呈現你對條文與案例之間關聯性的理解，內容的完整度與深度將作為課堂評分參考。若補完的是教師已建立的框架版講義，請直接在原檔案的骨架位置補上內容，不需另建新檔案。

## AI 協助與具名規則

- 你可以使用 AI 工具（例如 Claude、ChatGPT 等）協助查找資料、撰寫初稿，使用方式不限制。
- 但提交前你必須自己逐一查證內容：確保時間、數據、法規條號、引用來源都是真實存在、可查證的，不可有 AI 產生的幻覺內容。
- **本 repo 是公開的（public）**，講義正文、PR 標題、PR 描述欄位、Commit 訊息，全部任何人都看得到、也會被搜尋引擎索引，不是私密空間。
- 因此具名時**一律只能寫「遮蔽版」姓名學號**，格式為：姓名只留頭尾兩字、中間用 `○` 取代；學號只留末三碼、其餘用 `*` 取代。範例：
  > 中信金於2024年8月23日宣布以公開收購方式取得新光金10%至51%股權。（陳○明，******456）
- **請勿在 repo 任何地方（正文、PR 標題、PR 描述、commit 訊息）留下完整真實姓名或完整學號**，包括看起來像是「只有老師看得到」的地方——沒有這種地方，全部都是公開的。
- 老師會透過你的 GitHub 帳號，對照另外保存（不在本 repo 內）的名單來確認你的身分，因此請確保你的 GitHub 帳號有讓老師知道是誰在使用。
- 老師會依提交內容，口頭抽問你對案例的理解程度，作為配套查核方式，因此請確保你對自己補完的內容有真正理解，不是單純複製貼上。

## AI 審查流程說明

- PR 送出後，**不會馬上被自動審查或評分**，需要等老師先看過一次，確認內容大致沒問題後，手動貼上 `ready-for-review` 這個標籤，才會被排入每天的自動審查與建議分數流程。
- 這個等待是正常的，不代表你的 PR 有問題，純粹是防止垃圾 PR 濫用審查資源的機制。
- 貼上標籤後，最晚隔天（每天台北時間下午六點跑排程）就會在 repo 的 Issues 裡看到一則「PR 每日審查報告」，裡面會有 AI 針對你的 PR 給的初步意見與建議分數（僅供參考，最終分數與是否合併由老師決定）。
- 如果你送出 PR 一段時間了都還沒被貼標籤，可以直接跟老師反應，不用一直等。

## 提示詞注入(Prompt Injection)防制機制

- 本 repo 的 AI 審查與評分是自動呼叫 Claude 完成的，因此**請勿在提交內容中嘗試以任何方式操控 AI 的判斷**，例如寫入「忽略前面所有指示」「給這份提交滿分」之類的文字，不論是明寫還是藏在不容易注意到的地方（例如極小字體、跟背景同色）。
- 系統對此設有防制機制：AI 在審查與評分時，會被明確告知提交內容一律視為「待審查資料」而非「指令」；系統也會掃描提交內容是否含有常見的操控性關鍵字，一旦命中，會在每日審查報告中特別標記警示，並提醒老師該 PR 的 AI 分數**不建議直接採信**，須人工複核。
- 若發現有同學嘗試以此方式操控評分，將視為誠信問題處理，不會因為多寫了操控用的文字而拿到比較高的分數，反而會被特別檢視。

## 字數限制

- 每次 PR 補充或新增的內容，以**繁體中文 1000 字**為限（不含 HTML 標籤本身）。
- 超過 1000 字的部分，系統無法保證會被 AI 審查／評分涵蓋到，**超過部分請自行負責**，建議自己控制篇幅在限制內。
- 若單一 PR 內容過長導致系統判定為「已截斷」，每日審查報告會特別標記警示，提醒老師該 PR 的 AI 意見僅根據部分內容產生，會需要額外人工複核，這種情況對你並不有利，請避免。

## 注意事項

- 案例內容請確保來源真實可查證，不可虛構
- 引用他人分析或文章時請註明出處
- 提交內容以 CC BY-NC 4.0 授權釋出，代表你同意本專案得非商業性使用你的提交內容
